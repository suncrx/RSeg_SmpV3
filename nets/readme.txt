
munet :
50 epochs, train iou 0.85, val iou 0.781, without pretrained weights, batch 8

munet_ag:
50 epochs, train iou 0.85, val iou 0.775, without pretrained weights, batch 8

munet_cbam:
50 epochs, train iou 0.84, val iou 0.771, without pretrained weights, batch 8

munet1: bad performance
50 epochs, train iou 0.36, val iou 0.38, without pretrained weights, batch 8

munet2: 
50 epochs, train iou 0.77, val iou 0.75, without pretrained weights, batch 8

mxsegnet:
50 epochs, train iou 0.88, val iou 0.772, pretrained weights, batch 8

munetx :
50 epochs
train iou 0.72, val iou 0.740, without pretrained weights, batch 8, dropout0.5
train iou 0.85, val iou 0.776, without pretrained weights, batch 8, residual
train iou 0.89, val iou 0.787, without pretrained weights, batch 8, residual+se_attention
train iou 0.79, val iou 0.784, without pretrained weights, batch 8, residual+sk_attention
train iou 0.79, val iou 0.774, without pretrained weights, batch 8, residual+cbam_attention
train iou 0.79, val iou 0.782, without pretrained weights, batch 8, residual+coord_attention
train iou 0.78, val iou 0.773, without pretrained weights, batch 8, residual+GAM



------------------------------------------------------------------

unet_mobile_2:
50 epochs, train iou 0.85, val iou 0.769, pretrained weights, batch 8

unet_resnet50:
50 epochs, train iou 0.88, val iou 0.77, pretrained weights, batch 8

unet_resnet34:
50 epochs, train iou 0.88, val iou 0.772, pretrained weights, batch 8

unet_resnet18:
50 epochs, train iou 0.88, val iou 0.770, pretrained weights, batch 8

unet_vgg16:
50 epochs, train iou 0.85, val iou 0.755, pretrained weights, batch 4

unet_vgg19:
50 epochs, train iou 0.85, val iou 0.754, pretrained weights, batch 4

unet_resnext101_32x8d:
50 epochs, train iou 0.89, val iou 0.776, pretrained weights, batch 8

unet_resnest269e:
50 epochs, train iou 0.91, val iou 0.782, pretrained weights, batch 8

unet_densenet121:
50 epochs, train iou 0.88, val iou 0.773, pretrained weights, batch 8

unet_cbam_resnet50:
50 epochs, train iou 0.90, val iou 0.776, pretrained weights, batch 8

unet_cbam_resnet50:
50 epochs, train iou 0.88, val iou 0.775, pretrained weights, batch 8


unetpp_resnet50:
50 epochs, train iou 0.90, val iou 0.77~0.78, pretrained weights, batch 8

deeplabv3_resnet50：
50 epochs, train iou 0.78, val iou 0.69, pretrained weights, batch 8

pspnet_resnet50:
50 epochs, train iou 0.72, val iou 0.688, pretrained weights, batch 8

fpn_resnet50:
50 epochs, train iou 0.82, val iou 0.74, pretrained weights, batch 8


